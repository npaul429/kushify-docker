FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
RUN cp .env.local.backup .env.local 2>/dev/null || true

CMD ["npx", "vite", "--host", "0.0.0.0", "--port", "3001", "--no-open"]
